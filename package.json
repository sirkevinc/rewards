{
    "scripts": {
      "heroku-prebuild": "cd api && npm install && npm run migrate && npm run seed && cd ../client && npm install && npm run build",
      "start": "cd api && npm start && cd ../client && npm start"
  }
}
  